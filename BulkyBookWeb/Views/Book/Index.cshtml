@model IEnumerable<BulkyBookWeb.Models.book>





<div class="container p-4">
    <div class="row pt-4">
        <div class="col-6">
            <h1 clss="text-primary">BOOKS</h1>
        </div>
        <div class="col-6 text-end">
            <a asp-action="Create" class="btn btn-outline-success">
                <i class="bi bi-plus-circle-dotted"></i> &nbsp; Create New Book
            </a>
        </div>
    </div>
</div>
<br />

@* <table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Category)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody> *@
<div class="row row-cols-1 row-cols-md-3 g-4">
    @foreach (var item in Model)
    {
        <div class="col">
            <div class="card border-dark mb-3" style="max-width: 20rem;">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <span>@Html.DisplayFor(modelItem => item.Price)$</span>
                    @* <div>
                        <a class="btn btn-primary" asp-controller="Category" asp-action="buy" asp-route-id="@item.Category.Id" style="background-color: 
                           lightgray; border-color: lightgray;"
                           onmouseover="this.style.backgroundColor='darkgrey'; this.style.borderColor='darkgrey';"
                           onmouseout="this.style.backgroundColor='lightgray'; this.style.borderColor='lightgray';">Buy Now</a>
                    </div> *@
                </div>
                <div class="card-body">
                    <h4 class="card-title">@Html.DisplayFor(modelItem => item.Name)</h4>
                    <h4 class="card-text">@Html.DisplayFor(modelItem => item.Category.Name)</h4>
                    <div class="btn-group" role="group">
                        <a class="btn btn-group" asp-action="Edit" asp-route-id="@item.Id">Edit</a>
                        <a class="btn btn-group" asp-action="Details" asp-route-id="@item.Id">Details</a>
                        <a class="btn btn-group" asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                    </div>
                </div>
            </div>
        </div>
    }
</div>

        @* <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Category.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr> *@

@* 
    </tbody>
</table> *@
